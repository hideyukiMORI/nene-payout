# 2026-07-18

W1 初弾（完全準拠キャンペーン Phase 1・語彙 codemod 波の先頭）の発令日。ただし今日の主戦果は「撃った」ことではなく「**撃つ前に測ったら、的の大半がもう無かった**」こと——指示書と規約 05 §9.3 が前提にしていた payout の姿（dead class 17・known-utility 未配線・hooks/ 残存）は 07-14 実測のまま stale で、実際の main では4日間の先行 merge（#159/#160/#161/#164/#196）で全て解消済みだった。pre-stage 実測でこれを確定し、hub 裁定で発射内容を再定義。残っていた実欠陥＝消費者ゼロの dead token 5本を先行 PR-C で削除した。**マージ済み: PR #218（main=467362a）。PR-A は tokens 1.0.2 の npm publish 待ちで未着手。** 夜、ayane.co.jp リニューアルローンチ（フリート横断・payout 手番なし〔伝聞〕）。

---

## 朝: board reconcile（done化漏れ掃除・起動フック）

`board.sh reconcile` で候補5件。タイトル一致×全参照 closed の1件（payout「exemplar アンカー3本」＝07-16 W1送り決着済み）のみ done 化し、残り4件は ops残（nene-clear env 常設・nene-records API key）/ 進行中タスクの誤マッチ（nene-invoice #38=組織CRUD）/ 申し送り元疑い（nene-records #871）と判別して**意図的に open 維持**〔各 Issue タイトルと本文を gh で実読〕。hub 裁定＝done化・保留とも全件妥当。保留(3)(4) は hub 管理へ。

---

## W1 pre-stage 3点: 指示書の前提が全面 stale と実測で確定

指示書（W1 work order・hub 発行）の pre-stage を実施。

- **Step0（SHA ピン）**: origin/main = `0d37a60` を fetch・ピン〔実測〕。検出器フル実走 = eslint（known-utility 込み）/ stylelint / knip 全 green・**テスト 47 files / 185 passed**〔実測。§9.3 の公称「177」から増加〕。
- **旧語彙実測 — §9.3 payout 行（eb5f7aa・07-14 時点）は全面 stale**〔git log eb5f7aa..origin/main と grep で実測〕:
  - dead class 17 → **0**（第1波 PR #160 で text-primary/text-muted 17箇所・第2波 PR #164 で text-body/text-heading 36件、いずれも既 merge）。
  - **語彙 codemod 自体が適用済み**: PR #159（07-15 merge）が nene2-tokens@1.0.0 の versioned CLI で置換133件/41ファイル＋テーマ12トークンを適用済み（M-1 準拠＝PR 本文に codemod 名+version あり〔コミット本文実読〕）。@theme は契約語彙一色・COMMON_TABLE 旧キー残存 **0**〔grep〕。
  - known-utility lint 配線済み（#161・`...nene2.styling`）・hooks/→model/ 移設済み（#196）。
- **写像表 v1 素読**: 正本 = nene2-tokens `codemod-map.ts`（CODEMOD_MAP_V1）。payout 該当 = common 表＋REMEDIATION_V1 の payout 4項目（全て #160/#164 で処理済みと突合）。npm 公開は 1.0.0/1.0.1 のみで #35/#50 入り 1.0.2 は未 publish〔`npm view` 実測〕＝指示書の発射条件と整合。
- **新規発見の残欠陥**〔grep 実測〕: (a) 消費者ゼロの **dead token 5本**（`--font-x-family-sans` ほか font/line 系——由来 utility 使用 0件・`var()` 参照 0件）。(b) **Inter フォント未適用**: 'Inter' 宣言は初期実装の `--font-family-sans` 時代から utility `font-sans`（参照先は `--font-sans`）に一度も接続されておらず、全画面 Tailwind デフォルト sans で出荷中。`git log --follow` で before 形を実読し、**codemod の退行ではなく初期からの潜在バグ**と確定。(c) 契約外無言依存 `font-sans`×46・`font-medium`×40。
- 🔴 **自分のミスと捕まえ方**: 初回スクリーニングで `--radius-x-md` を「疑わしい旧導出トークン」群に誤分類した。トークン名でなく**由来 utility class**（`rounded-x-md`）で grep し直したら 8箇所で使用中の生きたトークンと判明し、削除対象から除外。「トークンの生死はトークン名でなくクラス綴りで測る」が正しい測り方だった。

疑問4件（PR-A の実体・PR-B/C の既済み扱い・Inter の帰属・§9.3 訂正の管轄）を理由付きで hub へ relay → 裁定: **PR-A = 1.0.2 re-run で diff 0（idempotence）を機械証明する実弾確認に純化・PR-B スキップ・PR-C = dead token 5本削除で GO・Inter と無言依存は AI-18 棚卸しへ・§9.3 訂正は hub 管轄**。

---

## 先行 PR-C: dead token 5本削除（#217 / PR #218 merged）

- Issue #217 → `fix/217-remove-dead-theme-tokens` → `default.css` から **-5行のみ**（値変更なし・使用中の color/shadow/radius/spacing 不変）。
- 証拠（M-3 機械出力）: 削除5トークンの残存参照 grep **0件**・`npm run check` exit 0（test 185 / knip / stylelint 含む）・`npm run build`（本体＋widget）成功〔全て実測〕。
- CI 3本 green（Frontend 1m12s / Backend 24s / gitleaks 6s〔実測〕）→ hub が diff 実読で APPROVE・merge GO〔伝聞〕→ squash merge。**main = `467362a`**〔実測〕。
- M-1 準拠: 手動是正の独立 PR（codemod 出力と非混載）。スコープ外（Inter 回復・無言依存）は PR 本文に明記。

---

## 送付2件（relay・本業の副産物）

- **AI-18 棚卸し材料3点**を hub へ: ①テーマの約束が typography で破れている実測 ②無言依存の現物量（46+40） ③契約 v2 で typography 座席を契約化しない限りテーマは文字造形を支配できない、という含意。hub が契約 v2 第二波の台帳に転記〔伝聞〕。#218 で宣言を消した後の「意図の正はこの台帳」も明記。
- **記事ネタ2本**を articles リナへ（施主の常設指示・同日発令): ①「テーマに 'Inter' と書いてもフォントは変わらない」②「codemod を撃つ前に的を測ったら、的がもう無かった」。いずれも一次資料（PR/Issue 番号・grep コマンド）付き。

---

## 📊 本日の数字

**自分で実測した数字:**

| 項目 | 値 |
| --- | --- |
| マージした PR | **1**（#218・-5行） |
| 立てた Issue | #217 / #219（本日報） |
| dead class 残存 | **0**（§9.3 公称 17 → grep 実測 0） |
| 旧語彙トークン残存（COMMON_TABLE キー） | **0** |
| 削除した dead token | 5本（由来 utility 使用 0件・var() 参照 0件） |
| `npm run check` | exit 0（test **185 passed** / knip / stylelint 含む・複数回） |
| PR #218 CI | 3本 green（Frontend 1m12s / Backend 24s / gitleaks 6s） |
| board reconcile | 候補5件 → done化 1・保留 4（全件 hub 裁定で妥当） |
| tokens npm 公開版 | 1.0.0 / 1.0.1（1.0.2 は未 publish・`npm view`） |

**渡された値を測り直したもの:**

| 項目 | 渡された値 | 実測 | 結果 |
| --- | --- | --- | --- |
| payout dead class | 指示書・§9.3「17（silent no-op）」 | grep 0件・#160/#164 で既是正 | **stale**（4日前の実測値） |
| known-utility lint | 指示書「PR-B で有効化」 | #161 で配線済み・green | **既済み**＝PR-B スキップ裁定 |
| hooks/ 残存 | §9.3「view-dashboard/hooks/ 残存」 | #196 で model/ へ移設済み | **stale** |
| テスト数 | §9.3「177」 | 47 files / 185 passed | 増加（8本） |

**伝聞（自分で測っていない）:** hub の #218 diff 実読 APPROVE・AI-18 台帳転記・vault への実弾先頭繰り上げ・ayane.co.jp リニューアルローンチ。

---

## ★教訓

- **文書の実測値には測定日の SHA が付いている——付いていない数字は疑う。** §9.3 の「dead 17」は eb5f7aa（07-14）の正しい実測だったが、4日で腐った。マルチエージェント並走では規約文書の数字は日単位で古くなる。発射前の再実測4コマンドで、空撃ち＋既済み作業の重複 PR 3本分を回避した。
- **dead class（使う側の空振り）には鏡像がある——dead token（宣言側の空振り）。** どちらも全ゲート緑のまま fail-silent で出荷される。「'Inter' と書いたのに一度も画面に出ていない」は後者の実例で、検出器は「未知クラスの使用」は見るが「未使用トークンの宣言」は見ていなかった。
- **トークンの生死は宣言名でなく由来 utility の綴りで測る。** `--radius-x-md` をトークン名 grep で dead と誤判定しかけ、クラス綴り（`rounded-x-md`）の grep で生存と確定した。測る対象の座標系（宣言側 vs 消費側）を先に決めてから grep する。

---

## 残（open）

| # | 内容 | 状態 |
| --- | --- | --- |
| — | **PR-A**（W1 最終手番: tokens 1.0.2 re-run で diff 0 証明＋lint 再実走証拠） | **publish 待ち**。fleet release 準備 → 施主 publish → hub「発射可」中継で即応 |
| — | i18n 移行＋exemplar アンカー3本 | nene2-i18n 0.2.0（./testing export）出荷後の後段（hub 中継待ち・07-16 決着のまま植えない） |
| — | Inter 適用回復・font-sans/font-medium 無言依存 | AI-18 棚卸し台帳（hub 管理）へ送付済み。payout 単独では動かさない |
| #170 | NENE_PAYOUT_API_URL の正本不在 | open（W1 と無関係・従来から） |
| #185 | typescript major の上流追跡 | open（上流監視・#130 HOLD 継続） |

payout 単独で今動かす手番は無し。tokens 1.0.2 publish の中継を待つ。

---

## セッション2（午後〜夜・統合リナ指揮下）

施主 hide が「統合リナに指揮を委任・依頼対応／疑問は hub へ／完了は hub へ報告+確認+レビュー」と指示。以降 hub の依頼を ccbridge で受けて処理した。本セッションで **W1 語彙波の payout を 1 番手クローズ**（PR-A 発射＋先行 merge の確認）、加えて Dependabot 精査・フロントテスト厳格化（T0/T1-lite）・日報 §8 自己点検を実施。

## Dependabot #212–216 精査（hub GO・patch/minor 緑は self-merge）

- #213 @vitejs/plugin-react・#214 globals・#216 typescript-eslint・#212 eslint 10.7 を self-merge。#212 は連続 merge で lockfile 競合 → `@dependabot rebase` → 再 CI 全緑を確認してから merge。
- #215 prettier 3.9.5 は CI fail が **整形差分のみ**と診断。hub 裁定 (a) で format-only の独立 commit（`style(frontend): …`）で reflow → 緑化 → merge（テスト数不変を確認）。

## #170 API_URL 正本一本化（PR #221・hub APPROVE）

Vite dev proxy 転送先を `NENE_PAYOUT_PORT` から導出して正本を一本化（`NENE_PAYOUT_API_URL` は escape hatch）。PORT 未設定時の既定 9000 不変を実測確認（unset→9000／PORT=9002→9002／API_URL→上書き）。

## フロントテスト厳格化 T0 / T1-lite（施主発令キャンペーン）

- **T0**（読み取り実測）: vitest 47 files/185 passed・CI required・**coverage 初計測**（`@vitest/coverage-v8` を `--no-save` で一時導入＝設定不変）。TOP5 未テスト重要ロジックを特定。MSW 2.12 の成熟ハーネス（server+7 handlers・factories・render helper）を雛形として供出。
- **T1-lite**（Issue #222・test-only・self-merge）: #223 auth-token/errors（+16）・#224 AuthGate fail-closed 回帰（+3・coverage 0%→カバー）・#225 session useLogin/useCurrentUser（+5）。狙いは行数でなく **セキュリティ分岐の固定**（isRetryable 境界・fail-closed・role fail-close）。

## 日報 §8 D2 自己点検（Issue #226・PR #227）

日報規約 v2 §8（docs/daily は公開物）の D2。07 月 daily の NG④（内部パス）3件を該当行のみ除去（履歴改変なし）。境界例2件は hub 裁定で原文維持。

## 🎯 W1 PR-A 発射＋クローズ（Issue #228 / PR #230）

`@hideyukimori/nene2-tokens` を exact **1.1.0** で pin。codemod は **theme 駆動**で payout の theme は #159 で全移行済み → **rename plan 空＝固定点**。knip は CLI 専用 devtool を unused 判定するため `ignoreDependencies` に追加（companion・同一軸）。**発射前に grep で「widget rounded-md 4件を codemod が rename」と誤報 → 権威手順 `codemod-plan`＝0 で自己反証**し、撃つ前に訂正。

### 📊 W1 payout M-3 完了判定（機械出力・hub 指示で記録）

```
① codemod --check（固定点）:
   $ npx nene2-tokens codemod --check --theme src/shared/ui/theme/themes/default.css --map common src
   → codemod@1.0.0 / mapping table v1.0.2 / table=common
   → plan: 0 token rename(s) → 0 class rename(s) / exit 0
② codemod-plan（M-1 記録）:
   → { codemodVersion:"1.0.0", mapVersion:"1.0.2", table:"common",
       tokenRenames:[], classRenames:[], unmapped:[] }
③ known-utility lint（active + green）:
   → probe（未知クラス注入）で better-tailwindcss/no-unknown-classes 発火
   → npm run lint = eslint . --max-warnings 0 ＝ warning で CI 赤＝実質 enforce・現コード緑
④ dead class grep 実測: 0
```

- **PR/SHA 束（完了根拠）**: PR-A=#230 / PR-B 相当=#161（known-utility lint 配線） / PR-C 相当=#160・#164・#218（dead class/token 是正）。
- PR-B/PR-C は **先行 merge で既済み**（新規 PR は no-op）と裏取りし、冗長 PR を作らず skip（hub 裁定）。
- 残: work order step7（i18n＋exemplar アンカー）は nene2-i18n 0.2.0 出荷後の後段でペンド。

## 📊 本日の数字（セッション2・すべて実測）

| 指標 | 値 |
| --- | --- |
| 本セッション merged PR | **11本**（#213/#214/#216/#212/#215/#221/#223/#224/#225/#227/#230） |
| フロントテスト | 185 → **209 passed**（+24・47→52 files） |
| coverage（`--no-save` 一時計測） | Lines 52.92→**53.46%** / Branches 50.96→**51.93%** |
| W1 payout | **クローズ**（1 番手・M-3 機械証拠上記） |
| 起票 | payout #222/#226/#228/#229/#231・nene2-fleet-tooling #98（公称17 stale） |

## ★教訓（セッション2）

- **発射前に権威手順で前提を検証する。** PR-A で「codemod が widget を書き換える」と grep で早合点したが、正準 `codemod-plan`＝0 で反証。theme 駆動 codemod は移行済み theme で固定点。撃つ前に確かめれば実害はゼロで、訂正を隠さず PR 本文に書くのが flagship の作法。
- **公称値には測定日が付く。** 05 §9.3 の「dead class 17」は 07-14 値で 07-18 は 0。stale な公称は W6 で偽赤の種 → fleet-tooling #98 に起票。
- **CLI 専用 devtool の pin は knip ゲートと衝突する。** `ignoreDependencies` に足すのが companion 定石（tailwindcss と同型）。knip ゲート付きリポは W1 pin で同対処が要る。

## 残（open・セッション2）

| # | 内容 | 状態 |
| --- | --- | --- |
| #229 | widget 4ファイルの `rounded-md` が off-contract（Tailwind 既定 0.375rem・contract 0.5rem） | 明朝の施主 docket（是正は視覚変化を伴うため意図確認要・W1 非交差） |
| — | W1 step7 i18n＋exemplar アンカー | nene2-i18n 0.2.0 出荷後の後段でペンド |
