# 日報 — 2026-07-16（NeNe Payout）

> **依頼された作業を1本もやらなかった日。** 「exemplar アンカー3本を植える」という施主 GO 済みの指示に対し、着手前に指し先を実測したところ**3本とも当の条文に違反しており、準拠する手段が存在しなかった**ため、植えずに止めた。代わりに出したのは「嘘を消す」PR 3本と、**W1 のクリティカルパスを塞いでいた道具のバグ2件**。**PR 3本マージ（#172 / #174 / #176）・Issue 6本・規約 patch 4件に反映・アンカー植栽 0本。**

## ヘッドライン

**「アンカーを植えれば規約と現物の乖離を機械が検出できるようになる」——これが成り立たないと分かったので、植えなかった（#169）。** 決め手は検出器の実装を読んだこと。`nene2-standards/src/checks/exemplars.ts:133` は `body.includes('[' + anchor + ']')` の**単純部分一致**で、**指し先の内容を一切照合しない**。つまり**アンカーは目印であって検出器ではない**。植えれば `check:exemplars` は green になるが、それは「内容を見ない検査器を green にするために、**I18N-6 に違反しているファイルへ『これが I18N-6 の exemplar』と書く**」行為であり、批准前提 (b) が実質を伴わない green になる。

実測した指し先の状態:

- **I18N-6 / `locales.ts`**: 条文が「`resolveLocale` を実装すること MUST NOT（package の責務）」と定めるのに、現物 `locales.ts:31` に実装があり3箇所から使用中。`SUPPORTED_LOCALES` も `catalogs` も無い。**一致するのは `DEFAULT_LOCALE = 'ja'` の1点のみ**
- **I18N-20 / `locales.test.ts`**: **準拠が物理的に不可能**。必須の `expectCatalogParity` を提供する `@hideyukimori/nene2-i18n` は `private: true` / `0.1.0-rc.1` / `npm view` → **E404（未 publish）**、しかも `exports` に規約が指定する `./testing` サブパスが存在しない
- **I18N-22**: `translate.ts:16` の `messages[key] ?? en[key]` が**規約の ❌ 負例と構造同一**（沈黙フォールバック・本番のフォールバック先が権威 ja でなく en）。**規約の負例は payout を名指ししておらず、同型であることは本日の新発見**（起草時の見落とし）

統合リナと (b)（植えない・W1 の i18n パッケージ移行に同乗）で合意し、**規約 `04-i18n.md:167` に現状注記が入った**。I18N-22 は `[X]` アンカー自体が無く**どのゲートにも掛からない**ことも条文に残った。

---

## 本日の成果（時系列）

### A. 「v2 で通信来てない？」— 経路の取り違えだった（コード変更なし）

施主の質問に対し、**私は「v2」を `@hideyukimori/nene2-client` transport のことだと思い込み**、frontend の env と Vite proxy を調べた。実際は chat-relay v2（push 型）の話。

v2 受信は健全だった（実測: bridge pid 1389573 が 8804 を LISTEN・親は自セッション 1389515・稼働中 bridge 4本が全て正しい持ち主に紐付き奪い合いゼロ・自己 push が `<channel>` として実描画）。**通信は来ていて、v1 inbox に 00:11:12 から未読で溜まっていた**。統合リナが v1（pull 型 inbox）で送り、私が v2（push）を見張っていた。

副産物として **Issue #170**（`NENE_PAYOUT_API_URL` の正本が無く、ポートを変えても Vite proxy が追随しない）を起票。施主判断で**記録のみ・実装しない**。

### B. #171 / PR #172 — token storage key を規約準拠へ（**merged** `7db4cf4`）

規約 `02-data-flow.md:86` が「命名は `nene_<product>_token` 固定」と定め、**payout の名前で正例を示している**のに、現物は `nene-payout-token`（ハイフン）だった。

**現物量の実測（`origin/main`）**: アンダースコア形 **8**（NENE2 / clear / contact / field / origin / profile / serve / vault）vs ハイフン形 **payout の1本のみ**。→ 規約が正しく payout が外れ値。

施主判断「ログアウトしてもいいよ」により migration shim は**実装しない**。変更はキー文字列のみ（構造の `createSessionTokenStore` 採用は `eb5f7aa` で既に規約どおり）。docstring の「same key as before」はリネームで嘘になるため併せて更新。

### C. #173 / PR #174 — 型権威が en のままという嘘の docstring（**merged** `37cd6db`）

`locales.ts:2-4` と `translate.ts:6-9` が「English is the (typed) source of truth」と書いていたが、`614fc29`（#162）で**型権威は ja へ反転済み**だった。`locales.ts` の一文は「型権威」と「実行時フォールバック」を**1文に束ねており、#162 は前者だけを動かした**ため片方だけが嘘になっていた。単純置換では直せず文を割った。コメントのみ・**ロジック差分ゼロを機械確認**（`git diff -U0` の増減行がコメント行のみであることを検査）。

### D. #175 / PR #176 — `check` に knip が無く検出器が一度も走っていなかった（**merged** `f84cf09`）

`"knip": "knip"` の script も devDependency も `knip.json` もあるのに `check` から呼ばれておらず、**dead code 検出器がフリートで payout だけ無効**だった。

**現物量の実測**: knip script を持つ10リポ中 **9リポが `check` に入れており、9/9 が `test` の後・最後尾**。順序を発明せず揃えた結果 `nene-profile` と完全一致の形になった。追加後の `check` を実行して **exit 0 を実測**（CI は緑のまま）。

`knip.json` の `ignore` に入っている `tokens.ts`（12行・**参照0件**）は**外していない**。ignore を外すと knip が `Unused files (1)` と正しく検出することだけ確認し、`knip.json` は復元（差分0）。**二重に隠れていた**（ignore + knip 自体が走らない）ので、本 PR は後者だけ直した。

### E. 🔴 批准前提 (c) の素振り — 4件の穴を見つけて止まった

- **(c) の定義を README が取りこぼしていた**: `README:116` は「W1 対象6リポへの素振り結果添付」だけだが、会議決定 `minutes:809` は「**validate:themes＋oracle** を…（**oracle 分は R5 の6系統実測が先行実施済み**）」。**README だけ読むと (c) が何をすることか分からない**
- **「W1 対象6リポ」の列挙が条文に無い**（Wave 表 `05:1161` ほか5本の独立根拠から `payout/vault/origin/field/invoice/records` と復元）
- **payout 単独では (c) は閉じない・1/6**（`r3-tokens:95`「この波の真の目的は6リポの是正ではなく **codemod と validate:themes 自体の実戦検証**」）
- **(c) の成果物に置き場が無い**（red リストは題からして `(a)(b)` 専用）

4件とも規約 patch に反映され、**(c) の判定も fail-closed（G-6）＝6リポ中1本でも素振り不能なら unknown** と明記された。**その最初の該当例が payout 自身**になった（下記 F）。

**payout の `validate:themes` 素振り実測**: **exit 1・3 errors**（`active.css`/`index.css` の pragma 欠落、`themes/default.css` の contract キー **23個**欠損）。「**no mechanical repair**・`fill` は root @theme に no-op」＝機械的に緑到達不能で、これは #159 の PR 本文が既に fleet#16 として起票済みだった。生データ保持中。

### F. 🔴 W1 のクリティカルパスを塞いでいた道具のバグ 2件

`board:57` の「同梱ランナーが #159 を byte 一致で再現できる（**228/228** — 前任の統合リナが測った数字）」を検証した。

1. **道具が自分の生成物を自分の検査器で拒否していた**: `mapTokenName('--font-weight-medium')` は `--font-weight-x-medium` を出すのに、`contract.ts` の `EXTENSION_TOKEN_PATTERN` はカテゴリを `[a-z][a-z0-9]*`（ハイフン不可）としており **`isExtensionTokenName` が false** → `generate` が exit 1・`validate` が TK-03。**W1 6リポ全部が該当**（`board:50` 実測「font-weight 400/500/600/700 は全リポ一致」）。fleet-tooling が `37b852d`（#49）で修正
2. **その修正は失敗を消さず移動させただけだった**: 修正後も `generate` は exit 1（今度は `--line-x-height-body`）。原因は構造的で、`tailwindNamespaceOf` は「**表に無ければ先頭セグメント群を返す**」fallback を持つのに、`EXTENSION_TOKEN_PATTERN` は表から `join('|')` で構築され **fallback を持たない**。**生成側に fallback があり検査側に無い**非対称が残っていた
   - さらに**同じ経路で片方は黙って通る**: `--font-size-body` → fallback `font` → `--font-x-size-body` は**表に 'font' があるので受理される**。しかし `--font-*` は **font-family の namespace**＝「font-family が 0.875rem」を意味する。**#17 のバグが font-size では生き残っている**。payout の origin/main は現にこれを出荷しているが、**参照0（dead）なので実害は出ていない**——それは #163 が `.text-body` を消した偶然による

施主裁定は **(i) 生成側の fallback を消して fail-closed に**（表に無い namespace は reject＝道具が名前を発明しない）。実装は fleet-tooling・明日。

### G. #159 の byte 一致（`board:57` の判断材料）

`eb5f7aa`（#159 の base）に worktree を切り、同梱ランナーで再現して `7d1e537`（#159）と突合した。

**結果: 211/228。byte 一致しない。** 差分17ファイル・27行で、中身は**全部1種類**（`font-medium` → `font-x-medium`）。**前任の「228/228」は再現しなかった**（228 の正体は payout frontend の `.ts/.tsx` 総数）。

⚠ **ただしこの測定は修正前の道具（#49 前）で行ったため無効**。道具が変わるので**再測が要る**。テーマ側は `generate` が完走しないため byte 比較は依然できない。

### H. codegen の横断調査 — payout は vault の「逆」だった（測定のみ）

依頼は「`npm run codegen` して `git diff` が出るか測れ」だったが、**payout に codegen は無い**（script も生成物もゼロ）＝実行不能。**vault の「生成型が腐る」問題は payout には存在しない。**

代わりに **7本の `api-types.ts` が手書き**で、`session/api-types.ts:1` が「Wire DTOs for the auth endpoints (**mirrors** `docs/openapi/openapi.yaml`)」と宣言している。**鏡だと宣言しているが、照合機構がゼロ**（CI も frontend も openapi.yaml を参照していない）。vault が「生成物が腐って検出器が黙る」なら、payout は「**そもそも生成せず人間が鏡を手写ししている**」。

**実際のずれ 2件**（実装せず報告のみ）:

- **enum の取りこぼし 2/6**: OpenAPI の enum 6本中、wire DTO で閉じているのは4本（`GatewayIdDto` / `ReceivedInvoiceStatusDto` / `PaymentExecutionStatusDto` / `SupportedLocale`）。**`Role` と `AccountType` だけ素の `string`**。**閉じたユニオンは同じ entity の隣に既にある**（`session/model.ts:8` の `Role`・`vendor/model.ts:3` の `ACCOUNT_TYPES`）＝**payout 内で不統一**
- **null 性のずれ**: `CurrentUserDto` の id/email/role が `string | null` だが OpenAPI は `required: [id, email, role]` で非 null（TS が契約より広い）
- なお `OrganizationDto` は**正本と完全一致**＝ずれは局所的で、系統的な崩壊ではない

`tokens.ts` の docstring も「Values must mirror `themes/*.css` — **never a second source of truth**」と言うのに照合機構が無い。＝**「鏡だと宣言しているが照合機構が無い」が payout に2件**。「条文があって機械強制が無い宙吊り」のコード版。

### I. #177 / 本 PR — 日報の1本目と、その規律の明文化

施主指示で各リポが日報を書くことになった。payout には慣習が無く**今日が1本目**。書式は `nene-records/docs/daily/2026-07-15.md` に寄せた。

統合リナから「**PR は要りません（直接 main へでも）**」と指示されたが、**採らなかった**。`CLAUDE.md:35-36` が「**Issue-driven**: no Issue, no code/**doc** change」「**never commit directly to `main`**」と明記しており、書式の正本である records の現物も **Issue 駆動 + PR**（`docs: 2026-07-15 の記録を current.md に同期する（#889）(#890)`）だったため。**今日ずっと「規約と現物のどちらが正か」をやってきて、最後の1本で規約を破るのは筋が通らない。** 施主に確認して Issue → ブランチ → PR で進めた。**その後、統合リナから同じ内容の訂正が届いた**（施主に指摘されたとのこと）。

**そして規律を CLAUDE.md に書いた。** 施主は「すべてのリポジトリに日報の慣習があって、可能な限り作業日に日報を書いてもらっている」と言うのに、**どのリポの CLAUDE.md にもその規律が無い**（records・payout・vault・fleet-tooling とも **0件**・実測）。＝**慣習は口伝で、読まれる場所に書かれていない**。だから統合リナは payout について**2回続けて外した**（「直接 main でよい」「日報の慣習が無い」）。**辿らないと分からない規律・書かれていない慣習は、次のリナが必ず踏む。**

**実測**（`daily` / `journal` / `diary` / `worklog` / `log` / `report` / `日報` / `作業記録` の綴りで全数検索）: payout に日報の慣習は**本当に無い**（`docs/milestones` は**空ディレクトリ**）。**Issue 駆動・`main` 直 commit 禁止は既に `CLAUDE.md:35-36` にあり追加不要。**

---

### J. 午後 — repo-status の齟齬を施主指示で是正（PR 3本・全て CI green・レビュー待ち）

施主に「隅々まで確認して状態を把握して」と言われ repo-status を回したところ、**午前とは別種の嘘**が出た。「修正しておいて」を受けて4件着手。

- **design-handoff 一式の退避**（git 操作なし）: `design-handoff/`（55ファイル）と zip が repo 直下にあり、ルート `CLAUDE.md` の「直下を汚さない」に違反。`_work/assets/nene-payout-design-handoff-2026-06-18{,.zip}` へ移動。日付は中身の生成日（6/18）に合わせ、命名は既存5本の並び（`<repo>-…-design-handoff-<date>`）に揃えた。
- **#179 / PR #180 — `.gitignore` に `.claude` 系が無かった**: `.claude/worktrees/pr-c-known-utility` は **385M の生きた登録済み worktree**（`git worktree list` に出る）。`git add -A` 一発で入る。
- **#181 / PR #182 — `current.md` が 1ヶ月・27コミット腐っていた**（下記 K）。
- **#122 / PR #123 — ウィジェットの `CONFLICTING` を解消**（下記 L）。

### K. `current.md` の棚卸し — 「knip 完了」は午前の #176 が暴いた嘘だった

`Last updated: 2026-06-17` のまま main に **27コミット**。`:8`「Storybook・**knip**・i18n 整理まで完了」と `:74`「knip #94」が完了扱いだったが、**#94 は導入しただけで `check` に未配線**＝午前の #176 まで一度も走っていない。**1ヶ月間「dead code 検出済み」と読める状態**だった。「**導入 ≠ 実行**」を本文に教訓として残した。

7月の W1 全12本（#153〜#176）を新設 W1 節へ。**未完3件もブロッカー付きで残した**（完了を増やすだけの棚卸しにしない）。ウィジェット #122/#123 の欠落も補充 — **`README:51` は正しく書いており、README のほうが正確という逆転**が起きていた。

テスト件数は `199/155` → **`222/181`**。**引き写さず自分で走らせた**（`OK (222 tests, 828 assertions)` / `Tests 181 passed`）。以後の更新者向けに「件数を更新するときは実際に走らせた出力を貼る」と明記。

### L. PR #123 — テキスト衝突は1箇所。**本体は lint の退行だった**

`CONFLICTING`・6/18 から約1ヶ月放置・+2,302行。main を取り込むと **衝突は `en.ts` の1箇所だけ**（widget の2キー vs #168 の `satisfies`）。両方残して解決。#162 で ja が型権威になったため ja/en のキー集合一致が `satisfies` の成立条件になるが、widget キーは **11本で完全一致**を確認。

**衝突が軽い代わりに lint が重かった。** main の baseline は **`check` exit 0（問題ゼロ）**なのに、取り込み直後は **5 errors / 64 warnings**。`lint` は `--max-warnings 0` なので warning も落ちる。ウィジェット枝は #160/#164（dead class 削除）と #161（known-utility lint）より**前に書かれたコード**だった。

**dead class 64箇所を #160/#164/#159 の写像どおり是正（写像は git 履歴から取得＝発明していない）**。移行先10種が main に実在することを事前実測し、`gap-x-stack-lg` だけ用例が無かったのでテーマ定義（`--spacing-x-stack-lg: 2rem`）まで確認してから使った。

残る **5 errors は全て `WidgetDemoPage.tsx`（dev 専用）由来**。直すには overlay 色と iframe 寸法を**テーマ契約へ足す設計判断**が要り、`validate:themes` が既に contract キー23個欠損で赤（fleet#16）の状況に更に足すことになる。**施主判断でデモページを本 PR から外した**（ルート・導線・i18n 7キーも併せて除去）。Q1 として統合リナ経由で施主判断に上がっている。

### M. 🔴 chat-relay の `ok` を着弾の証拠として扱ってしまった

14:30 に統合リナへ完了報告を送り、`ok -> :8790` を受け取って**届いたつもりで作業を締めた**。実際は hub が 13:56〜15:00 に「`ok` を返しつつ捨てる」状態で、**私の報告は落ちていた**。統合リナからの復旧連絡で発覚し、15:05 に再送。

**再送時に裏を取った**: 8790 の LISTEN は pid 160482・**起動 14:54:25**（親 10033 = `claude --mcp-config .../hub.mcp.json … server:cc-hub` ＝ hub 本体）。**私の送信 14:30 より後**＝当時受け皿が無かったことが実測で裏付いた。以後、重要な報告には **ack を求める**運用にする。README への注記を提案し、統合リナが**知見7 として追記済み**（`ok` は ccbridge が HTTP を受けた時点で返る＝allowlist 判定も bind 状態も見ていない、が核心）。

### N. 統合リナの回答で、書いたばかりの `current.md` が古くなった

15:00 過ぎ、統合リナから「**nene2-i18n 0.1.0 は publish 済み**（07-16 14:46 JST 施主実行）」。**PR #182 が未マージのまま嘘を抱えるところだった**ので、渡された数字を鵜呑みにせず自分で実測して直した（`1a97e8c`）。

**実測で2件足せた:**

- **`expectCatalogParity` は root から import 可能**（`npm pack` して `index.d.ts` を実測 — `export { checkCatalogParity, expectCatalogParity } from './parity.js'`）。→ 今朝の私の結論「**I18N-20 は準拠が物理的に不可能**」は**もう正しくない**。「実体は使える。規約が指す import 経路（`./testing`）と実体（root）がズレているだけ」＝**ブロッカーが1段軽くなった**
- **`resolveLocale` は export されていない**（root の公開 API は `createTranslator` / `checkCatalogParity` / `expectCatalogParity` と型のみ）。→ **I18N-6 は publish されても依然 red が正**。条文は「実装は package の責務」と言うが、**package がまだ引き取れない**ので `locales.ts:31` の実装は移す先が無い

残ブロッカーは publish ではなく **W0b（`./testing` サブパス ＋ `resolveLocale` の提供）** に絞り込めた。**統合リナが「認識は合ってる」と確認**し、この実測を仕分けに使うとのこと。

### O. 締め — 4本マージ・385M の掃除・そして🔴自分の数字が間違っていた

**15:55 JST に PR 4本が全てマージ**（#178 / #180 / #182 / #123 — 全て MERGED を自分で実測）。
main 側の実物も確認した: `.gitignore` の3行が入り、`src/Widget/*` が入り、**`widget-demo` は入っていない**（grep 0件＝デモ外しが効いている）。

**385M worktree を掃除**（`.claude` 385M → **28K**）。統合リナから「掃除可・実測で確定」と来たが、
**385M の削除なので鵜呑みにせず独立に照合した**:

- squash `e4e07ac`（04:37:25）は tip `4886007`（04:20:53）より **17分新しい**
- 共通 base `c462cc0` から見て**ブランチが触ったのは3ファイルのみ**（`eslint.config.js` / `package.json` / `package-lock.json`）で、**3つとも squash とバイト一致**
- squash 側の25ファイルは #164（dead class 第2波）が後から入ったぶんで、ブランチは元から未接触
→ **ブランチにしか無い作業ゼロ**を確認してから削除。ブランチ ref は残した（復元可能）

**#180 の実弾検収**: `git add -A` → 入るのは `.claude/skills/run-app/SKILL.md` の**1本（4K）だけ**。
**今朝この同じコマンドが 385M を stage した**ので、同じ弾を撃って直ったことを確かめた。
統合リナが issues #51 の他リポ展開時に「検収手順」としてこれを引く、とのこと。

#### 🔴 そして、自分が書いた数字が間違っていた

**`current.md` と本日報の両方に「Dependabot 13本」と書いた。正しくは 12本。** 両方マージ済み。

**原因**: 初回計測で `gh pr list --state open --jq length` が返した **14** から、
目視で「#178 と #123 を引いて 13」と**暗算した**。実際は 14 = **12 Dependabot + #178 + #123**。
**引き算を実測で確かめず、13 という数字を作った。**

さらに Issue 起票のために内訳を機械分類したところ、**major は typescript 1本ではなく4本**だった
（#130 typescript 5.9→7.0 / #128 @types/node 22→26 / #124 checkout 6→7 / #132 cache 5→6）。
統合リナの段階計画は「メジャーは typescript だけ」を前提にしていたので、**前提ごと修正**した（#183）。

→ 是正: #183 / 計画: #184 / typescript: #185 / 残メジャー3本: #186。**実施は (c) レーンの声掛け後。**

## 📊 本日の数字

**自分で実測した数字:**

| 項目 | 値 |
| --- | --- |
| マージした PR | **7**（午前 #172 / #174 / #176、午後 **#178 / #180 / #182 / #123** — 全て MERGED を実測確認） |
| レビュー待ちで残す PR | **0**（本日分は全て main 入り。残る open 12本は全て Dependabot） |
| 立てた Issue | **12**（#169 / #170 / #171 / #173 / #175 / #177 / #179 / #181 ＋ 締めの **#183 / #184 / #185 / #186**） |
| 🔴 **自分が間違えた数字** | **1**（Dependabot「13本」→ 正 **12本**。暗算し、2文書に書き、報告し、マージまで通した — O） |
| `.claude` の掃除 | **385M → 28K**（削除前に「ブランチ固有の作業ゼロ」を独立照合） |
| #180 の実弾検収 | `git add -A` → 入るのは skill **1本（4K）**のみ。**今朝は同じコマンドが 385M を stage した** |
| **植えたアンカー** | **0** |
| コード変更の総量 | 午前 **5ファイル**（キー文字列2・docstring 2・`check` の1行）／午後は下記 |
| #123 の是正量 | **10ファイル**（163 insertions / 218 deletions）— dead class **64箇所**・デモページ除去 |
| #123 の検証（自分で実行） | `composer check` **exit 0**（235 tests / 849 assertions）・`npm run check` **exit 0**（185 tests / 47 files） |
| main の baseline（自分で実行） | `check` **exit 0**（問題ゼロ）→ #123 取り込み直後は **5 errors / 64 warnings** |
| `.claude` 未 ignore の現物量 | **18リポ中 17**（ignore は NENE2 のみ）。payout は 385M で最大だが**外れ値ではない** |
| skill を commit している現物量 | **3リポ**（clear / origin / suite の `run-local/SKILL.md`）→ NENE2 の丸ごと ignore を写さない根拠 |
| current.md の腐り | **27コミット / 約1ヶ月**（Last updated 2026-06-17） |
| 退避した handoff | **55ファイル**（`_work/assets/` へ・移動前後で件数一致を確認） |
| token key の現物量 | アンダースコア **8** vs payout **1**（＋ concierge は `nene_admin_token` ＝製品名の別種逸脱） |
| knip の現物量 | 10リポ中 **9** が `check` に入れる・**9/9** が `test` の後 |
| `validate:themes`（payout） | **exit 1**・3 errors（pragma 2 / contract キー **23** 欠損） |
| #159 の byte 一致 | **211/228**（⚠ 修正前の道具・**再測が要る**） |
| `tokens.ts` | **12行・参照0**・ignore を外すと knip が検出 |

**渡された数字を、自分で測り直したもの:**

| 項目 | 統合リナから渡された値 | 自分の実測 | 結果 |
| --- | --- | --- | --- |
| nene2-i18n の publish | `0.1.0` / 07-16 14:46 JST | `npm view` → `0.1.0` / `time.created` `2026-07-16T05:46:12Z` | **一致** |
| `exports` の `./testing` | 「不在（W0b）」 | `npm view … exports` → `{ '.': {…} }` のみ | **一致** |
| root の公開 API | 「catalog+parity」 | `npm pack` → `index.d.ts` の 4 export を確認 | **一致＋2件足せた**（下記） |

**実測して初めて分かった2件**（統合リナへ返送済み・「認識は合ってる」と確認済み）:

- `expectCatalogParity` は **root から import 可能** → **I18N-20 は「物理的に不可能」から「経路ズレ」へ降格**
- `resolveLocale` は **export されていない** → **I18N-6 は publish されても依然 red が正**（移し先が無い）
| enum の取りこぼし | OpenAPI 6本中 **2本**（Role / AccountType）が wire で開いている |
| 自分の誤り | **5**（全て自分で捕まえた） |

**渡された数字（自分で測っていない）:**

- 統合リナ: 今日のフリート全体のマージ **11本**・統合リナの誤り **30回**・担当リナ3人が止めた **29回**（fleet-tooling 12・payout **11**・vault 6）
- 前任の統合リナ: #159 の byte 一致 **228/228**（→ **再現しなかった**）

---

## ★教訓

**今日フリート全体の誤りが1つの型に帰った: 「手元にあるものを、名指ししたものだと思い込む」。** 規約 `02` の **A-10「準拠判定の正は CI（origin/main）。ローカル grep は参考値」は、まさにこの事故への再発防止として制定された条文**なのに、今日10回踏まれた。

**私の5回は全部その変種で、しかも1つの型に収まる: 「グレップのパターンが対象を捉えていない」。**

1. **v2 を `nene2-client` transport だと思い込んだ**（施主の質問を取り違え・env と Vite proxy を延々調べた）
2. **`minutes:809` を読み直さず「文書間の矛盾がある」と診断**（全文は最初の grep で手元にあった。矛盾は無かった）
3. **「1.0.2 のリグレッション」と誤診**（コードのコメントを読む前に言った。実際は #17 の意図的修正で、追随漏れは `contract.ts` 側）
4. **x- トークンの参照数を token 名で測って「全部0参照」**（クラス名は別物: `--radius-x-md` → `rounded-x-md`。実際は **123箇所 live**）／**`--include` が効かず CSS 自身を数えた**
5. **enum を `': 'a' | 'b'` のプロパティ位置パターンで引いて「6 enum 全部が素の string」と結論しかけた**（**型エイリアス** `export type X = 'a' | 'b'` を見落としていただけで4本は閉じていた。「決済プロダクトなのに payment status が型で守られていない」と**誤報する寸前**）
6. **ローカルの `dist` を「今の道具」だと思って測った**（統合リナの「1.0.2 は未 publish」という訂正がきっかけで rebuild し、`isExtensionTokenName` が false → true に変わって気づいた。**今日ずっと戒めてきた罠を自分が踏んでいた**）

`tee` / `grep` 越しの `$?` を `validate` の exit だと読みかけた件も同型（報告前に捕まえた）。

**統合リナを止められたのは、賢かったからではなく、言う前に一度測ったから。** 逆に測らずに言った 1・2 はそのまま外した。**同じコインの裏表**であり、止めた回数と外した回数は同じ性質から出ている。

**同じことが最後にもう一度起きた。** 統合リナの「日報は PR 不要・直接 main へでも」は、**訂正が届く前に自分で捕まえた**——`CLAUDE.md:35-36` と records の現物（#889 → #890）を読んだから。読まずに従っていれば、**今日ずっと「規約と現物のどちらが正か」を測ってきた当のセッションが、最後の1本で自分のリポの規約を破っていた**。指示が誰から来ても、**正本は手を動かして確かめる**。

**「実害ゼロ」と言うときは「何に守られているのか」を聞くべきだった。** I18N-22 の実害がゼロなのは `en.ts:285` の `satisfies`（I18N-9）と parity テストが en の全域性を担保しているからで、**payout は I18N-22 の逸脱を I18N-9 で埋め合わせている**。条文間の依存が暗黙で、I18N-9 を外す変更が I18N-22 の事故を**遠隔で発火**させる。この論証は規約 `04:167` に採録された。

**午後の教訓**

**「コンフリクト解消」を git の衝突と同一視していた。** #123 のテキスト衝突は `en.ts` の1箇所だけで、`git merge` は5分で終わった。**本体は git に見えない退行**で、`check` が 5 errors / 64 warnings。「`MERGEABLE` になった」で止めていたら、CI が赤い PR を「直した」と報告するところだった。**マージできることと、動くことは別。**

**測定装置のバグを、対象の欠陥として報告しかけた。** #123 の検証で worktree に vendor を symlink したところ、composer の PSR-4 が実パス（本体の `src/`＝Widget が無い）を指し、**「`WidgetTokenService` not found」9件＋ CapabilityResolver の failure 1件**が出た。ブランチの欠陥として書きかけたが、10件が全部同じ「クラスが無い」形なのが不自然で気づいた。本体の作業ツリーで測り直したら **235 tests 全 green**。**赤が出たら、まず自分の測り方を疑う。**

**`ok` を着弾の証拠として扱った（M）。** 今日いちばん素朴なミス。道具の戻り値が何を保証しているかを確かめず、「エラーが出ていない＝届いた」と読んだ。**午前に「導入 ≠ 実行」を教訓として書いた当のセッションが、午後に「`ok` ≠ 着弾」で同じ穴に落ちた。** 抽象は同じ — **成功の証拠でないものを成功の証拠として使わない。**

**🔴 一日中「測っていない数字を書くな」と言い続けて、最後に自分が測っていない数字を書いた（O）。** これが今日いちばん重い。`14 - 2 = 13` と暗算し、その 13 を `current.md` と日報に書き、**統合リナにも「13本」と報告し、両方マージまで通した**。誰も気づかなかった。捕まえたのは Issue を立てるために**内訳を機械分類したとき** — つまり「本数を数える」のではなく「**中身を測る**」作業をして初めて出た。副産物として **major が1本でなく4本**だったことも判明し、統合リナの段階計画の前提ごと修正になった。

教訓は「暗算するな」ではない。**引き算にすら実測を要求する規律が、なぜ他人の数字にだけ働いて自分の数字には働かなかったのか**。今日 knip の「完了」も `translate.ts:16` も `ok` も hub の「25ファイル」も、**疑ったから**測った。13 は**疑わなかったから**測らなかった。**検算は、疑ったときにやることではなく、書く前に必ずやること。**

**書いた直後に自分の文書が古くなった（N）。** publish は私が `current.md` を push した約1時間後に起きていた。**文書の腐りは過去の怠慢だけでなく、進行中にも発生する**。今日 27コミット分の腐りを直した張本人が、同じ日に1時間で腐らせた。未マージのうちに捕まえられたのは、統合リナの回答を**そのまま信じず測ったから**。

---

## 残（open）

| # | 内容 | 状態 |
| --- | --- | --- |
| #170 | `NENE_PAYOUT_API_URL` の正本が無い | 施主判断で**記録のみ**・実装しない |
| #122 | 埋め込み決済ウィジェット | **PR #123 が `MERGEABLE`・CI green**（午後 L）。**レビュー待ち** |
| #179 | `.claude` 未 ignore | **PR #180**・レビュー待ち。**同じ穴がフリート17リポ → 統合リナが横断で引き取り**（issues 起票済・clear リナも独立に検出） |
| #181 | `current.md` の棚卸し | **PR #182**・レビュー待ち（`1a97e8c` で publish 実測を追記） |
| — | **Q1: overlay token の置き場**（デモページの 5 errors） | fleet#16／契約 v2 と絡むフリート token 設計のため**施主判断に上がった**。回答まで `WidgetDemoPage` は #123 から外したままが正 |
| — | **385M worktree の実体掃除** | squash 済み #161 との内容照合を**統合リナが実施**。**触らない** |
| — | Dependabot **12本**（最古 6/20）＝ patch/minor **8** ＋ **major 4**（#130 typescript 5.9→7.0 / #128 @types/node 22→26 / #124 checkout 6→7 / #132 cache 5→6） | **Issue 起票済み**（計画 #184・major #185/#186）。**実施は (c) レーンの声掛け後**。~~13本~~ は私の暗算ミス（#183・下記 O） |
| — | **(c) の素振り**（payout 分 1/6・生データ保持中） | **(c) 節の生成器の形が決まってから**。現状は道具が完走しないので **fail-closed で unknown** |
| — | **`board:57` の byte 一致再測** | **道具が変わるので必須**（211/228 は無効）。道具（fleet PR#50）は**施主承認待ちで未マージ**＝**マージ後に統合リナから合図が来る。それまで再測不要**（07-16 15:30 同期） |
| — | **`tokens.ts` の処遇** | `api-types.ts` と同じ「鏡だと宣言しているが照合機構が無い」球。**2件まとめて1本の issue が筋**（統合リナが施主へ） |
| — | **W1 のテーマ移行** | 施主裁定 (i) reject の実装（fleet-tooling）待ち。実装後、payout は **typography で正直に落ちる**形になる（第二波・due 08-21） |
| — | **アンカー3本** | **W1 の i18n パッケージ移行に同乗**。~~ブロッカーは publish~~ → **07-16 14:46 に publish 済み（自分で実測）**。残ブロッカーは **W0b（`./testing` サブパス ＋ `resolveLocale` の提供）** に絞り込み、統合リナが「認識は合ってる」と確認。**I18N-6 は W0b まで red が正**・**I18N-20 は「物理不能」→「経路ズレ」に降格**（root import で実体は使える。手書き3テストの置換は W1 採用作業として手番が来たら） |

**明日の統合リナの指示書に、上記の球が載る予定。**
