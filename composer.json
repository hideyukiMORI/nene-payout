{
    "name": "hideyukimori/nene-payout",
    "description": "Self-hosted vendor invoice payment via credit card — NeNe Payout",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.4.1 <9.0",
        "hideyukimori/nene2": "@dev"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.95",
        "phpstan/phpstan": "^2.1",
        "phpunit/phpunit": "^13.1",
        "robmorgan/phinx": "^0.16.11",
        "symfony/yaml": "^8.0"
    },
    "repositories": [
        {
            "type": "path",
            "url": "../NENE2"
        }
    ],
    "autoload": {
        "psr-4": {
            "NenePayout\\": "src/"
        },
        "files": [
            "src/bootstrap.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "NenePayout\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit --testsuite NenePayout",
        "analyse": "phpstan analyse --memory-limit 512M",
        "cs": "php-cs-fixer fix --dry-run --diff --config=.php-cs-fixer.php",
        "cs:fix": "php-cs-fixer fix --config=.php-cs-fixer.php",
        "openapi": "php tools/validate-openapi.php",
        "migrations:status": "phinx status -c phinx.php",
        "migrations:migrate": "phinx migrate -c phinx.php",
        "migrations:rollback": "phinx rollback -c phinx.php",
        "check": [
            "@test",
            "@analyse",
            "@cs",
            "@openapi"
        ]
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "php-http/discovery": true
        }
    }
}
